package be.ipeters.rabbitmq;

/* carlpeters created on 06/04/2023 inside the package - be.ipeters.queue */

import lombok.extern.slf4j.Slf4j;

//import org.appsdeveloper.domain.MenuOrder;
//import org.springframework.amqp.rabbit.annotation.RabbitHandler;
//import org.springframework.amqp.rabbit.annotation.RabbitListener;
//import org.springframework.stereotype.Component;
//@Component
@Slf4j
//@RabbitListener(queues = "rabbitmq.queue", id = "listener")
public class RabbitMQReceiver {
//    private static Logger logger = LogManager.getLogger(RabbitMQReceiver.class.toString());
//    @RabbitHandler
//    public void receiver(MenuOrder menuOrder) {
//        log.info("MenuOrder listener invoked - Consuming Message with MenuOrder Identifier : " + menuOrder.getOrderIdentifier());
//    }
}
